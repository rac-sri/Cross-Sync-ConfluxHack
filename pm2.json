{
    "apps": [
        {
            "name": "crossSync",
            "exec_mode": "cluster",
            "instances": "min",
            "script": "server/",
            "interpreter": "ts-node"
        }
    ]
}